name: "Hello world"

on:
  push:
    branches:
      - v4

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo current time
        run: echo "the current service time is $(date)"
